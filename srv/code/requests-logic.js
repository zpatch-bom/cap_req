/**
 * lohhhhh
 * @On(event = { "createDraft" }, entity = "cap_request.Requests")
 * @param {Object} request - User information, tenant-specific CDS model, headers and query parameters
*/
module.exports = async function(request) {
	if (aa == 1) {
	} else {
	}
}